<?xml version="1.0" encoding="UTF-8"?>

<beans  
    xmlns="http://www.springframework.org/schema/beans"  
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"  
     xmlns:context="http://www.springframework.org/schema/context"
    xmlns:p="http://www.springframework.org/schema/p"  
    xsi:schemaLocation="http://www.springframework.org/schema/beans   
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context-3.0.xsd">
  

    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close" >
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <context:property-placeholder location="jdbc.properties"/>
    

<bean id="employeeDAO" class="com.mize.dao.EmployeeDAOImpl" autowire="byType">
<property name="dataSource" ref="dataSource"></property>
<property name="queries">
<value>
selectAll=select * from new_employees
selectById=select * from new_employees where empid=?
selectByDepartment=select * from new_employees where department=?
insert=insert into new_employees values(?,?,?,?,?)
updateEmployee=update new_employees set name=?,department=?,salary=?,joindate=? where empid=?
deleteEmpById=delete from new_employees where empid=?
getAvgSal=select avg(salary) from new_employees
getAvgSalByDep=select department,avg(salary) from new_employees group by department
getEmpCount=select count(*) from new_employees
</value>

</property>
</bean>
<bean id="service" class="com.mize.service.EmployeeServiceImpl" autowire="constructor" />
<bean id="employeeDTO" class="com.mize.dto.Employee" autowire="byType"/>
</beans> 
